#include "holberton.h"
/**
 * add - function that adds two integers and returns the result
 * @i: interger to add
 * @j: integer to add
 *
 * Return: result of two ints added
 */
int add(int i, int j)
{
	int a = i + j;

	return (a);
}
