#include "holberton.h"
/**
 * mul - function that multiplies two integers.
 * @a: first paraemeter to multiply
 * @b: second parameter to multiply
 *
 * Return: the multiplied number of a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
